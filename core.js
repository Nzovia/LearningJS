//arrays
let members = ["Peter", "Chris", "Faith", "Frank"]
//displaying the elements in arrays
console.log(members[0])
//length of the array is larger than one from the indexed
//ararys in javascript you can mix as long as you want they are called composite datatypes pr complex types
let me = ["nicholas", 25, true]
//you can only items at the end of an array using the push  method
members.push("Benson")

//create an array of messages
let messages = ["Good morning", "Yes, how are you", "i am fine, you", "am fine too"]
message.push("Have a good day")
message.pop()
console.log(messages)

//for loops in javascript
for (let count = 1; count < 11; count += 1) {
    //3hours continue

}